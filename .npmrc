# Supply-chain hardening (local development).
# CI/CD intentionally overrides min-release-age via NPM_CONFIG_MIN_RELEASE_AGE=0
# and gates installs with `npm audit` instead.
ignore-scripts=true
min-release-age=7
